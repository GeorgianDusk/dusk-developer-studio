# Dusk Developer Studio comprehensive validation — final

## Verdict

The exact public release **v1.0.19** is production-ready for the currently available DuskDS and DuskEVM pre-launch scope. All 32 fresh clean-state source-blind Codex developer pilots passed, all 107 confirmed defects were retested and verified, eight suspected defects were invalidated with qualification and evidence, the exact npm bytes passed Windows, Ubuntu and macOS assurance, and two fresh independent challenge reviews were clear. No unapproved P0–P3 defect remains.

This is rigorous Codex-operated product simulation and machine evidence, not recruited human usability research.

## Exact release identity

- Source/tag: `91bb1c923250ccdcd3a79f69c1604a7ab9e2b392` / `v1.0.19`
- npm: `dusk-developer-studio@1.0.19`
- npm integrity: `sha512-SwCaaHg8ta0J76w7nmvTPTgpk+Ddv1d6hVB/2AzulfhvK4PiUap0LW8rmuiCwysrkNL0kEjJtMv9nuxYmheZAw==`
- tarball SHA-256: `9a9701a2c017ef5b0434466912bd64942cb3ca0a85db77e4478d7279c69a40c2`
- package inventory SHA-256: `358df51b7b032f33d5bdbf188dfe377ba33624f13db0780fe2c6bb5e0e8a22b2` (29 payload files; 30 tar entries including the self-excluding inventory manifest)
- production: https://studio.134-122-59-217.nip.io
- production release manifest SHA-256: `05219022f770d266b4fde46ccaaa87a78ee5ec8db5396aaae827e995870b2efc`

## What passed

| Evidence area | Result |
|---|---:|
| Source-blind clean-state pilots | 32 / 32 |
| Core end-to-end | 8 / 8 |
| npm release lifecycle | 6 / 6 |
| UI, accessibility and browsers | 6 / 6 |
| Failure and recovery | 6 / 6 |
| Security and containment | 6 / 6 |
| Confirmed defect retests | 107 / 107 |
| Invalidated suspected defects | 8 / 8 |
| Required product surfaces with success, recovery and exact-candidate evidence | 44 / 44 |
| Native package platforms | Windows x64, Ubuntu 24.04 x64, macOS 15 arm64 |
| Fresh final challenges | 2 / 2 clear |

The browser group covered Chromium, Firefox and Playwright WebKit; desktop, mobile and breakpoint widths; 200% zoom; keyboard-only operation; forced colors; reduced motion; accessibility trees; route-wide checks; destructive-confirmation recovery; external-doc return; and the complete DuskDS UI journey. Runtime and security pilots covered install, provenance, first run, reinstall, upgrade, both fixed-port conflicts, Safe and Local Actions, pairing and stale sessions, direct and UI starter creation, interruption, concurrency, path and filesystem boundaries, hostile requests and inputs, secret redaction, detached descendants, cleanup and preservation.

The v1.0.19 pilot campaign used new isolated sessions and new candidate-bound receipts. Engine claims are limited to the browser engine actually exercised, native-runner claims distinguish direct observations from authenticated CI receipts, and any initial partial or invalid observation remains visible in its source bundle rather than being relabelled as a pass.

## Production and release assurance

The exact registry package has a verified npm signature and SLSA provenance bound to this repository, tag, workflow and commit. Cross-platform assurance exercised the same immutable tarball on Windows 2025, Ubuntu 24.04 and macOS 15. The guarded static release completed after an A→B→A rollback rehearsal; the final public checks verified TLS, health, assets, SPA fallback behavior, source links, the exact release fingerprint, closed development/companion ports and a fresh read-only DuskDS Testnet node observation.

The final security campaign reverified bounded bootstrap bodies and then retested the v1.0.19 containment fixes discovered during the candidate pilots. The exact release disables Rustup auto-install during preflight, invalidates private scaffold paths across session generations, terminates detached POSIX descendant groups before wrapper cleanup, and guards the delayed Windows fallback against completed-child PID reuse. Source regressions, exact-package assurance, native runners, source-blind hostile pilots, and independent process/listener observations bind those fixes to the public candidate.

The guarded static release passed a non-mutating dry run, an A→B→A rollback rehearsal, a separate real promotion, and independent production checks. The release receipt binds the exact v1.0.19 commit and manifest fingerprint; the rollback receipt binds the restored v1.0.18 predecessor.

The local companion contains tracked child processes and process groups, enforces bounded time and output, excludes secret-bearing environment values, closes its loopback ports, and cleans disposable project state. A fully untracked process deliberately detached by the same OS user outside the tracked process group remains an explicit operating-system boundary rather than a sandbox guarantee.

## DuskEVM boundary and activation checklist

DuskEVM Testnet is not live, so wallet connection, RPC, signing and deployment cannot honestly be validated yet. The production journey was verified as a truthful educational pre-launch surface: it does not claim a live chain or activate machine actions.

When DuskEVM Testnet becomes available:

1. Publish the authoritative RPC URL, chain ID, explorer, wallet support and test-faucet guidance.
2. Verify TLS, `eth_chainId`, latest-block reads and controlled outage/retry behavior from hosted and local paths.
3. Exercise wallet connection without handling private keys, then deploy and read a disposable contract on the reviewed Testnet.
4. Add exact success, refusal, wrong-chain, stale-wallet, network-loss and recovery evidence.
5. Rerun browser, security, accessibility, package, production and challenge gates before changing pre-launch labels.

## Evidence

The machine-readable ledger is `docs/evidence/comprehensive-validation-evidence-2026-07-20.json`. Its unique receipts are under `docs/evidence/comprehensive-validation-receipts/`. The final gate independently rebinds the evidence-only descendant commit to tag `v1.0.19`, the exact registry tarball, the immutable first-attempt main-push artifact, a fresh authenticated re-download, native GitHub Actions evidence and production observations.
