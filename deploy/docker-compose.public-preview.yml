services:
  dusk-studio:
    build:
      context: .
      dockerfile: Dockerfile
    image: dusk-developer-testnet-studio:public-preview
    restart: unless-stopped
    ports:
      - "127.0.0.1:8080:8080"
